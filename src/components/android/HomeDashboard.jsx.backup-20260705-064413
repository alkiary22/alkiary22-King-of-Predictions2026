import { Link } from "react-router-dom";
import { CalendarDays, Radio } from "lucide-react";

export default function HomeDashboard() {
  return (
    <section className="px-4 pt-1 pb-6">

      <div className="grid grid-cols-2 gap-3">

        <Link
          to="/matches"
          className="h-20 rounded-xl bg-[#171717] border border-[#D4AF37]/15 px-3 py-2 flex flex-col justify-between"
        >
          <CalendarDays className="text-[#D4AF37]" size={18} />

          <div>
            <h3 className="text-sm font-black">
              المباريات
            </h3>

            <p className="text-[10px] text-zinc-400">
              جميع المباريات
            </p>
          </div>
        </Link>

        <Link
          to="/user-predictions"
          className="h-20 rounded-xl bg-[#171717] border border-[#D4AF37]/15 px-3 py-2 flex flex-col justify-between"
        >
          <Radio className="text-[#D4AF37]" size={18} />

          <div>
            <h3 className="text-sm font-black">
              التوقعات
            </h3>

            <p className="text-[10px] text-zinc-400">
              توقعات المستخدمين
            </p>
          </div>
        </Link>

      </div>

    </section>
  );
}
