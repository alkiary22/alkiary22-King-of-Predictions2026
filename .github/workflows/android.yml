name: Build Android Release APK

on:
  workflow_dispatch:
  push:
    branches:
      - feature/api-football-leagues

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/api-football-leagues
          fetch-depth: 0

      - name: Show exact commit
        run: |
          echo "===== BUILDING BRANCH ====="
          git branch --show-current

          echo "===== BUILDING COMMIT ====="
          git rev-parse HEAD
          git log -1 --oneline

          echo "===== VERIFY COMPETITION CODE ====="
          grep -n -A18 -B3 'seasonLabel:' src/components/competitions/CompetitionGrid.jsx || true
          grep -n -A8 -B3 'competitionSeason' src/components/competitions/CompetitionStandings.jsx || true

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Build current web version for Capacitor
        run: |
          echo "===== BUILDING CURRENT WEB VERSION ====="
          REACT_APP_BACKEND_URL=https://kingbackend-apk.onrender.com \
            CI=false npm run build

          echo "===== BUILD SIZE ====="
          du -sh build

          echo "===== VERIFY BACKEND ====="
          grep -Rao 'https://kingbackend-apk.onrender.com' \
            build/static/js/*.js | sort -u

          echo "===== VERIFY GOOGLE ID TOKEN ====="
          grep -Rao 'getIdToken' \
            build/static/js/*.js | head -10

          test -f build/index.html

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21

      - name: Install Capacitor Android
        run: npm install @capacitor/core @capacitor/cli @capacitor/android --legacy-peer-deps

      - name: Restore Keystore
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE }}" | base64 --decode > android/app/KingPredictions.jks

      - name: Show Release Certificate SHA
        run: |
          echo "===== RELEASE CERTIFICATE ====="
          keytool -list -v \
            -keystore android/app/KingPredictions.jks \
            -storepass "${{ secrets.ANDROID_KEYSTORE_PASSWORD }}" \
            | grep -E "Alias name:|SHA1:|SHA256:"

      - name: Create key.properties
        run: |
          cat > android/key.properties <<EOF
          storeFile=KingPredictions.jks
          storePassword=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          keyAlias=${{ secrets.ANDROID_KEY_ALIAS }}
          keyPassword=${{ secrets.ANDROID_KEY_PASSWORD }}
          EOF

      - name: Sync Android
        run: npx cap sync android

      - name: Build Release APK
        run: |
          chmod +x android/gradlew
          cd android
          ./gradlew clean assembleRelease

      - name: Prepare APK
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)

          cp android/app/build/outputs/apk/release/app-release.apk \
            "King-Predictions-${SHORT_SHA}.apk"

          echo "APK_NAME=King-Predictions-${SHORT_SHA}" >> "$GITHUB_ENV"

          echo "===== FINAL APK ====="
          ls -lh "King-Predictions-${SHORT_SHA}.apk"

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APK_NAME }}
          path: King-Predictions-*.apk
          if-no-files-found: error